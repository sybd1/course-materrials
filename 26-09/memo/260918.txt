260918

비타민 먹어MPOT
과제 차근차근 순서대로 풀어서 보기
return 소괄호 내부에 JSX 문법 사용

1. 01_event-handling (마지막 부분)

컴포넌트의 핵심은 재사용하여 효율적으로 하는 것이다.

부모 쪽에서 props를 자식에게 제공함으로서 재사용 효율을 올린다.




2. 01_rendering-list

map을 사용해서 <li></li> 목록으로 반환할 수 있다. forEach는 반환값이 없어서 사용할 수 없다.
map으로 배열되는 콜백함수 2번째 매개변수는 index로 반환하기 때문에 <li></li> 내부에 index key값을 바로 넣을 수 있다.



3. 02_list-add-remove
구조 분해할당 무얼 위해 하는지
어떤 변수를 어떤 역할로 선언하는지

배열
1. 사용자에게 입력받을 밸류 값
2. 입력 받아 출력할 인덱스와 텍스트
3. 입력 받아 변경될 값s

실제 동작 기능
1. 타이핑 받아 바뀌는 밸류
2. 추가 버튼 누를 시 일어날 기능
3. 삭제 버튼 누를 시 일어날 기능



4. Hooks / 01_useEffect-basic, 02_useEffect-mount
Hooks (함수의 집합)
useState 상태값 처리
useEffect 렌더링 이후에 처리

useEffect(() => {})
핵심은 진행하는 순서이다.

빈 의존성 배열 []: 매 렌더링마다 실행되지 않음.
주로 초기 데이터 로딩(API 호출) 등에 사용
값 지정 의존성 배열: 

처음 마운트 직후 실행됨

lifecycle에 필수로 필요하다
