260922

비동기, 동기 작업????????
async 비동기: 시간이 걸리는 작업(서버 통신, 타이머 등)을 컴퓨터 백그라운드에 맡겨두고, 작업 완료를 기다리지 않고 바로 다음 코드로 넘어갑니다.
sync 동기: 앞선 코드가 완전히 끝날 때까지 다음 코드가 멈춰서서 기다린다.


.then 체이닝???
"이 작업이 다 끝나고 나서" 라는 뜻


Promise???????? 비동기 작업을 담은 객체인가?


increase???????

.catch() ????????

서버와 엔진의 관계?

ok????? 무슨 프로퍼티인가???

- 03_async-await

async/await: 이 작업이 다 끝나고 하겠다. function 앞에 async를 붙여줘야한다. 두 개는 세트다. 함께 있지 않으면 작동을 하지 않는다.

Ajax: 페이지를 새로고침 하지 않고 비동기적으로 필요한 데이터만 주고 받는 기술의 통칭
Fetch API: 그 도구 중 하나이다. 브라우저 내부에 기본적으로 제공하는 기능이다.


HTTP 에러: 제대로 된 주소값을 받아오지 못 함.
404(요청을 했는데 리소스가 없을때), 505 에러를 fetch는 제대로 reject 처리 하지 못하므로 if (!response.ok) 같은 boolean 연산자로 방어 코드를 이용해 잘 걸러내야한다

항상 실패했을때를 대비한 코드를 작성해야한다.

throw???????? 어디로 던지는 거지 catch로 던지는 건가?????????

fetch??????

try ???????????

- 03_axios
axios를 사용하면 HTTP에러를 자동으로 reject 해준다. catch에서 처리해주기만 하면 된다
json 파싱이 자동으로 되어 response.data에 담겨있음


