2609010

method: 객체 안에 존재하는 함수

프로토타입 문법 정확하게 외우기.
Member.prototype.introduce = function() {}
내가 실수한 것
Member.prototype.introduce() = function {}

#### Array 배열

[] Square Brackets 배열 안에 존재하는 값들을 요소 element라고 부른다.

- .length : 'length'는 객체가 기본으로 가지고 있는 내장 프로퍼티. 기능은 객체 안의 글자 수 카운트, 배열 안 요소 카운트

배열 메서드 
push 뒤 추가
pop 뒤 제거
unshift 앞 추가
shift 앞 제거
indexOF
includes

화살표 함수
const circle = x => x * x;
변수선언 변수 = 
console.log(circle(3)) 	// 9

forEach 각 요소를 돌며 특정 동작을 단순히 실행. 반환값 없음.
map 각 요소를 돌며 새로운 배열로 가공/변환.
 메서드 다시듣고 개념 이해하기.
배열의 고차함수, 콜백함수 이해하기

중괄호를 사용해야 객체이다.

forEach, map???
filter 조건에 맞는 모든 값
find 조건에 맞는 첫번째 값
some 하나라도 맞으면 true
 every 모두 맞아야 true

콜백함수? 고차함수?